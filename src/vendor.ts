// Vendors. Tricky word that is. Hard to find a definition for it.
// Import 3rd party libraries in here. For example jQuery, Lodash, angular2-jwt.
//
// Unless you plan on chunking vendors files for async loading. You would need
// to import the async loaded vendors at the entry point of the async loaded file.
//
// Also see custom-typings.d.ts as you also need to run `typings install x`
// where `x` is the imported module.

// Angular 2
import 'angular2/platform/browser';
import 'angular2/core';
import 'angular2/http';
import 'angular2/router';

// Firebase
import 'firebase';

